[build]
builder = "nixpacks"

[deploy]
startCommand = "cd cycle_clock_v2 && PYTHONPATH=/app/CCT-StandardModel:$PYTHONPATH python server.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
